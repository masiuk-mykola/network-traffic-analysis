# Where this app reaches the capture API. Server-side only — the browser never talks to the API,
# so this must not be prefixed with NEXT_PUBLIC_. Copy this file to .env.local to run outside Docker.
CAPTURE_API_URL=http://localhost:8700
